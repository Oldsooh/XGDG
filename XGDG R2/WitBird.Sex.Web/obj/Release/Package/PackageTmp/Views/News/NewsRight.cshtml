@model WitBird.Sex.Web.Models.NewsRightModel

@if (Model.RecommendNews != null && Model.RecommendNews.Count > 0)
{
    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title"><i class="glyphicon glyphicon-thumbs-up"></i>&nbsp; 推荐资讯</h3>
        </div>
        <div class="panel-body">
            @foreach (var item in Model.RecommendNews)
            {
                string url = "/news/" + item.Id + ".html";
                <div class="media md-jj">
                    <div class="media-left media-middle">
                        <a href="@url">
                            <img class="media-object" src="@Uitlity.WebHelper.GetImagePath(item.Cover)" alt="@item.Title">
                        </a>
                    </div>
                    <div class="media-body">
                        <h4 class="media-heading">
                            <a href="@url" title="@item.Title">@item.Title</a>
                        </h4><p>
                            分类：<a href="/news/" title="最新资讯">
                                最新资讯
                            </a>
                        </p>
                        <p>
                            <span><i class="fa fa-clock-o"></i>&nbsp;@item.InsertTime.ToString("yyyy-MM-dd")</span>&nbsp;&nbsp;
                            <span><i class="fa fa-eye"></i>&nbsp;@item.ViewCount</span>
                        </p>

                    </div>
                </div>

            }
        </div>
    </div>
}

@if (Model.TopNews != null && Model.TopNews.Count > 0)
{
    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title"><i class="glyphicon glyphicon-thumbs-up"></i>&nbsp; 一周排行</h3>
        </div>
        <div class="panel-body">
            @foreach (var item in Model.TopNews)
            {
                string url = "/news/" + item.Id + ".html";
                <div class="media md-jj">
                    <div class="media-left media-middle">
                        <a href="@url">
                            <img class="media-object" src="@Uitlity.WebHelper.GetImagePath(item.Cover)" alt="@item.Title">
                        </a>
                    </div>
                    <div class="media-body">
                        <h4 class="media-heading">
                            <a href="@url" title="@item.Title">@item.Title</a>
                        </h4>
                        <p>
                            分类：<a href="/news/" title="最新资讯">
                                最新资讯
                            </a>
                        </p>
                        <p>
                            <span><i class="fa fa-clock-o"></i>&nbsp;@item.InsertTime.ToString("yyyy-MM-dd")</span>&nbsp;&nbsp;
                            <span><i class="fa fa-eye"></i>&nbsp;@item.ViewCount</span>
                        </p>
                    </div>
                </div>

            }
        </div>
    </div>
}
